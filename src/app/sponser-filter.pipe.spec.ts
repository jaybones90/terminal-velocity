import { SponserFilterPipe } from './sponser-filter.pipe';

describe('SponserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SponserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
