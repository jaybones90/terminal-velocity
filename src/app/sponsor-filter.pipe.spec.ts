import { sponsorFilterPipe } from './sponsor-filter.pipe';

describe('sponsorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new sponsorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
